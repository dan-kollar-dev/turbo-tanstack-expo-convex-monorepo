# Use pnpm's default isolated mode (no hoisting)
# This allows each app to have its own React version without conflicts

# Expo and React Native require hoisting for compatibility
public-hoist-pattern[]=*expo*
public-hoist-pattern[]=*react-native*

strict-peer-dependencies=false

