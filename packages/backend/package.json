{
  "name": "@repo/backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "type-check": "tsc --noEmit",
    "lint": "echo \"No linting configured\"",
    "clean": "rm -rf .convex"
  },
  "dependencies": {
    "convex": "^1.11.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@repo/shared": "workspace:*"
  }
}

